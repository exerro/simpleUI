package com.exerro.simpleui

/** Specifies the alignment of an entity within a context with free space.
 *  Values should range from 0 to 1, where 0 indicates its default position
 *  (e.g. top/left) and 1 indicates its most deviant position
 *  (e.g. right/bottom). A value of 0.5 would indicate central alignment, and
 *  the value is continuous between 0 and 1.*/
typealias Alignment = Float
